language: node_js

brances:
    only:
        - master

sudo: false

node_js:
    - "0.12"
    - "iojs"

services:
    - mongodb

notifications:
    email:
        on_success: never
        on_failure: never
