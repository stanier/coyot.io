<<<<<<< HEAD
.panel.panel-default(ng-controller='ServerCtlr' ng-init='getPkgs()')
    .panel-heading Packages
    .panel-body
        form.form-inline: .form-group
            button.btn(type='button' ng-click='currentPage = currentPage*1 - 1'
                ng-disabled="currentPage*1 <= 0 ? 'disabled' : ''") Previous
            
            button.btn(type='button' ng-click='currentPage = currentPage*1 + 1'
                ng-disabled="currentPage*1 >= (pkgs.length / pageSize) - 1 ? 'disabled' : ''") Next
=======
extends /templates/page

block body
    script.
        var hostname = '#{hostname}';
        var host = '#{host}';
        var port = '#{port}';
    
    .col-sm-3
        +panel_server
>>>>>>> 9b57247... Finished renaming, standardized navbar inclusion
        
            input.form-control(ng-model='currentPage')
            
        table.table
            thead
                td: h5 Package
            tbody
                tr(ng-repeat='pkg in pkgs | ' +
                        'offsetBy: pageSize * currentPage | ' +
                        'limitTo: pageSize')
                    td: a(href='../package/{{ pkg }}') {{ pkg }}
